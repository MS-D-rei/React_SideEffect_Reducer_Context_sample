import styled from 'styled-components';
import { StyledCard } from '@/components/UI/Card/StyledCard';

export const StyledCardHome = styled(StyledCard)`
  width: 90%;
  max-width: 40rem;
  padding: 3rem;
  margin: 2rem auto;
  text-align: center;
`;
